module.exports = { //since this is being called like a module, the inside file needs to look like a module

    ping: function() { //this is how a function would look like, name on the left followed by function() with optional arguments.
        return 'hello!'; //for example, this will pass 'Hello!' to the file using the ping() function
    }

}